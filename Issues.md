
Why and what to improve:

1) Too many created objects being a main overhead
2) string operations in functions
3) insane map usage on children
4) LOL THE DELETE ALGORITHM IS SO BAD
5) JSON SERIALISATION NEEDS MAJOR RE-WORK (like im not sure if i can even make it better)
4) this actually returns a value, so i might make a version of this that does only just validation

This project was made with writing the fundamental functional state in mind when it comes to working with systems